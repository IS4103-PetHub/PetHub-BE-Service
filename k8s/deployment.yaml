apiVersion: apps/v1
kind: Deployment
metadata:
  name: pethub-uat-deployment
  labels:
    name: pethub-uat-deployment
    app: pethub-uat-app
spec:
  replicas: 2
  selector:
    matchLabels:
      name: pethub-uat-pod
      app: pethub-uat-app
  template:
    metadata:
      name: pethub-uat-pod
      labels:
        name: pethub-uat-pod
        app: pethub-uat-app
    spec:
      containers:
        - name: pethub-uat
          image: "614839482597.dkr.ecr.ap-southeast-1.amazonaws.com/test-repository:latest"
          imagePullPolicy: Always
          resources:
            limits:
              memory: 512Mi
              cpu: "1"
            requests:
              memory: 128Mi
              cpu: "0.2"
          livenessProbe:
            httpGet:
              path: /api
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: pethub-uat-service
spec:
  selector:
    app: pethub-uat-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer
